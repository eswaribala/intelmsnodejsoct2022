import type elasticsearch from '@elastic/elasticsearch';
import { ElasticsearchInstrumentationConfig } from './types';
import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
export declare class ElasticsearchInstrumentation extends InstrumentationBase<typeof elasticsearch> {
    static readonly component = "@elastic/elasticsearch";
    protected _config: ElasticsearchInstrumentationConfig;
    private _isEnabled;
    private moduleVersion;
    constructor(config?: ElasticsearchInstrumentationConfig);
    setConfig(config?: ElasticsearchInstrumentationConfig): void;
    protected init(): InstrumentationModuleDefinition<typeof elasticsearch>;
    private patchObject;
    protected patch(operationClassName: string, moduleExports: any, moduleVersion: string): any;
    protected unpatch(moduleExports: any): void;
    private wrappedApiRequest;
    private _callOriginalFunction;
    private _addModuleVersionIfNeeded;
}
//# sourceMappingURL=elasticsearch.d.ts.map