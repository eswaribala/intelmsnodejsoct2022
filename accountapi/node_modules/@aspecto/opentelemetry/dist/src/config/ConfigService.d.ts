import { PrivacyRule } from '../privacy-rules/types/PrivacyRule';
import { DataCollectionMode } from '../privacy-rules/types/DataCollection';
import { LeanSamplingRule } from '@aspecto/sampling-rules';
interface Config {
    privacyRules: PrivacyRule[];
    collectorUrl: string;
    otCollectorUrl: string;
    jaegerEndpoint: string;
}
declare class ConfigService {
    private socket;
    connect(token: string): Promise<void>;
    private checkConnection;
    getConfig(): Promise<Config>;
    subscribeToSamplingRules(callback: (rules: LeanSamplingRule[]) => void): Promise<void>;
    subscribeToDataCollectionRules(callback: (rules: PrivacyRule[], mode: DataCollectionMode) => void): Promise<void>;
}
declare const _default: ConfigService;
export default _default;
//# sourceMappingURL=ConfigService.d.ts.map