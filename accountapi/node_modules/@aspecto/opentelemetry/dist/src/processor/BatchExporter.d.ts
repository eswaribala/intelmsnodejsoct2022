import { ReadableSpan, SpanExporter } from '@opentelemetry/sdk-trace-base';
export default class BatchExporter {
    private readonly exporter;
    private readonly bufferSize;
    private readonly bufferTimeout;
    private batchedSpans;
    private timerId;
    private ongoingExports;
    constructor(exporter: SpanExporter, bufferSize: number, bufferTimeout: number);
    /**
     * Add the span to a batch which will be sent
     *
     * @param {ReadableSpan} span
     * @memberof BatchExporter
     */
    exportSpan(span: ReadableSpan): void;
    forceFlush(): Promise<void>;
    shutdown(): Promise<void>;
    private maybeStartTimer;
    private flush;
    private clearTimer;
}
//# sourceMappingURL=BatchExporter.d.ts.map