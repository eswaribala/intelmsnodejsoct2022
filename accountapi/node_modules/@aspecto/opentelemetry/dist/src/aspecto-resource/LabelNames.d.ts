export declare enum LabelNames {
    /** A unique uuid with set for each instance of aspecto tracing collector. */
    HOSTNAME = "aspecto.hostname",
    VERSION = "aspecto.version",
    TOKEN = "aspecto.token",
    RUNTIME_VERSION = "aspecto.runtime.version",
    CI_REPORT = "aspecto.ci_report"
}
/** Not in spec - thus copied here. once added to spec, we can use value from semantic conventions instead */
export declare enum GitResourceAttributes {
    VCS_COMMIT_ID = "vcs.commit.id",
    VCS_CLONE_ID = "vcs.clone.id",
    VCS_BRANCH_NAME = "vcs.branch.name"
}
//# sourceMappingURL=LabelNames.d.ts.map