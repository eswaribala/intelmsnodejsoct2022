import { Resource } from '@opentelemetry/resources';
import AspectoClientOptions, { Logger } from '../interfaces/AspectoClientOptions';
import { SyncDetector } from 'opentelemetry-resource-detector-sync-api';
export declare class AspectoSyncDetector implements SyncDetector {
    private sdkOptions;
    private fileOptions;
    constructor(sdkOptions: AspectoClientOptions, fileOptions: AspectoClientOptions);
    detect(): Resource;
}
export declare const getAspectoConfigValue: (sdkOptions: AspectoClientOptions, fileOptions: AspectoClientOptions, optionName: keyof AspectoClientOptions, envVarName: string) => string | number | boolean | Logger;
//# sourceMappingURL=aspecto-resource.d.ts.map