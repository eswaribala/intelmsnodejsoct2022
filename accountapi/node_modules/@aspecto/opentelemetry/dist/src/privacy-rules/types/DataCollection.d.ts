import { ReadableSpan } from '@opentelemetry/sdk-trace-base';
import { ActionResult } from '../ActionResult';
import { CollectFieldAction, RuleAction, RuleCondition } from './PrivacyRule';
export interface DataCollectionAction {
    checkCondition(condition: RuleCondition, span: ReadableSpan): boolean;
    executeAction(actions: RuleAction[] | CollectFieldAction[], span: ReadableSpan, properties?: Record<string, any>): ActionResult;
    updateSpan(span: ReadableSpan, properties?: Record<string, any>): void;
}
export declare enum DataCollectionMode {
    BLOCK_LIST = "BLOCK_LIST",
    ALLOW_LIST = "ALLOW_LIST"
}
export declare const payloadAttributeName: {
    MESSAGING_PAYLOAD: string;
    LAMBDA_RESPONSE: string;
    HTTP_REQUEST_BODY: string;
    HTTP_RESPONSE_BODY: string;
    DB_RESPONSE: string;
};
//# sourceMappingURL=DataCollection.d.ts.map