import { Context } from '@opentelemetry/api';
import { SQSRecord, SNSEventRecord } from 'aws-lambda';
interface PatchSqsPayload {
    messages: SQSRecord[];
    faasRootContext: Context;
    queueUrl: string;
    queueName: string;
}
export declare const patchSqsRecords: ({ messages, faasRootContext, queueUrl, queueName }: PatchSqsPayload) => void;
interface PatchSnsPayload {
    messages: SNSEventRecord[];
    faasRootContext: Context;
    topicName: string;
}
export declare const patchSnsRecords: ({ messages, faasRootContext, topicName }: PatchSnsPayload) => void;
export {};
//# sourceMappingURL=pubsub-patcher.d.ts.map