import { LeanSamplingRule } from '@aspecto/sampling-rules';
import { Context, Sampler, SamplingResult, SpanKind, SpanAttributes, Link } from '@opentelemetry/api';
import { TraceIdRatioBasedSampler } from '@opentelemetry/core';
export interface SamplingRuleWithSampler extends LeanSamplingRule {
    sampler: TraceIdRatioBasedSampler;
}
export declare class RulesSampler implements Sampler {
    private ratio;
    private packageName;
    private env;
    private localSampler;
    private rules;
    constructor(ratio: number, packageName: string, env: string, requireConfigForTraces: boolean);
    setRules(rules: LeanSamplingRule[]): void;
    getMatchingRule(spanName: string, attributes: SpanAttributes): SamplingRuleWithSampler | undefined;
    private onFirstRulesConfig;
    private gotRules;
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: SpanAttributes, links?: Link[]): SamplingResult;
    toString(): string;
}
//# sourceMappingURL=RulesSampler.d.ts.map