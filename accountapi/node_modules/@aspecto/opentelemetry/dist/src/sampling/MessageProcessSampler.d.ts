import { Context, Link, Sampler, SamplingResult, SpanAttributes, SpanKind } from '@opentelemetry/api';
import { RulesSampler } from './RulesSampler';
export declare class MessagingProcessSampler implements Sampler {
    private rulesSampler;
    private serviceSampler;
    constructor(rulesSampler: RulesSampler, serviceSampler: Sampler);
    shouldSample(_context: Context, _traceId: string, _spanName: string, _spanKind: SpanKind, attributes: SpanAttributes, links: Link[]): SamplingResult;
    private shouldSampleProcessing;
    toString(): string;
}
//# sourceMappingURL=MessageProcessSampler.d.ts.map